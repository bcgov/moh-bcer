FROM node:14.17.6-slim

ENV PATH $PATH:/usr/src/app/node_modules/.bin

WORKDIR /usr/src/app

COPY . /usr/src/app

RUN rm -rf node_modules
RUN npm install 
RUN npm run build

EXPOSE 4000

CMD [ "npm", "run", "start" ]