# prepare-push          		 - Prepare the repository for pushing to the repo

prepare-push:
	@echo "+\n++ Make: Preparing modules for pushing to repository...\n+"
	@rm -rf dist
	@npm run build

build:
	@echo "+\n++ Building Shared Components"
	@npm i
	@npm run build
	@echo "+\n++ Finished building shared components"
