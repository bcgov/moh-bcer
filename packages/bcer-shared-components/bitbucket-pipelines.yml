image: node:24.14.1

pipelines:
  pull-requests:
    '**': # All branches
      - step:
          caches:
            - node
          name: Build and Test
          script:
            - echo "Installing dependencies..."
            - npm install
            - echo "Testing..."
            - npm run test
